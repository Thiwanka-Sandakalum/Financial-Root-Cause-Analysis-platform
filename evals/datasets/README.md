# Evaluation Datasets

Store evaluation datasets here as JSON lists.

Each example supports:

- `question` (required)
- `reference` (optional, enables correctness/recall style metrics)
- `ticker` (optional)
- `period` (optional)
- `category` (optional, exported in CSV)
